# Python fixtures

Structure scaffold for Python VSA fixtures. Per `../README.md` and `../MIGRATION-STATUS.md`
(Phase 3), the first Python fixture is `valid/01-todo-simple/` — to be created from scratch.

`valid/` and `invalid/` exist (with `.gitkeep`) so the fixture directory structure matches the
documented layout and `test_fixture_directory_structure_exists` passes; the fixtures themselves
are pending migration.
